import React from 'react'

type Props = {}

const ProductForm = (props: Props) => {
  return (
    <div>ProductForm</div>
  )
}

//we need to do the images after we add in all the other data

//Step 1: Enter Parent Product Details

//Step 2: Add + --> Enter Product Child Details including sizes and availability

//Step 3: Add Product Images, we perform validation on the format and type

//Step 4: Display Parent, Display added children, and give option to add another

//Nothing saves until the end so they can update and change any of the data